{ config, lib, inputs, ... }:

{
    # home.stateVersion = "21.03";
    imports = [ ../../modules/default.nix ];
    # modules = {
    #     firefox.enable = true;
    #     fish.enable = true;
    #     git.enable = true;
    #     stow.enable = true;
    # };
    
}
