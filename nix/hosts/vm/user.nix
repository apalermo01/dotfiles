{ config, lib, inputs, ... }:

{
    # home.stateVersion = "21.03";
    imports = [ ../../modules/default.nix ];
    # config.modules = {
    #    fish.enable = true;
    #    git.enable = true;
    #    stow.enable = true;
    #};
    
}
