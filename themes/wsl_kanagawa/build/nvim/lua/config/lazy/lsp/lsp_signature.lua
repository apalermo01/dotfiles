return {
    "ray-x/lsp_signature.nvim",
    event = "InsertEnter",
    opts = {
        bind = true,
        move_cursor_key = "<C-w>w",
        -- extra_trigger_chars = {},
        -- trigger_on_newline = false,
        -- timer_interval = 99999,
        -- toggle_key = "<M-s>"
    },
}
